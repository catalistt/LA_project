<br>
<%= form_with(model: payment, local: true) do |form| %>
  <% if payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :Orden, class: "control-label"%><br>
    <%= form.select(:order, order_with_name, {}, class:"form-control", id: 'js-searchable-payment', multiple: true)%>
  </div>

  <div class="form-group">
    <%= form.label :Método_de_pago, class: "control-label"%><br>
    <%= form.collection_select :payment_method_id, PaymentMethod.all, :id, :name, prompt: true%>
  </div>

  <div class="form-group">
    <%= form.label :Monto_pagado %><br>
    <%= form.number_field :amount_payed %>
  </div>

  <div class="form-group">
    <%= form.label :Estado %><br>
    <%= form.select(:status, order_status) %>
  </div>

  <div class="actions">
    <%= form.submit "Registrar", class:"btn btn-info" %>
  </div>
<% end %>
