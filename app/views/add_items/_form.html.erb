<div class="container ml-3">
    <div class="row">
      <div class="col-4"><br>
      <%= form_with(model: add_item, remote: true) do |form| %>
        <% if add_item.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>
            <ul>
              <% order.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="field">
          <%= form.label :Producto, class:"field" %>
          <%= form.select(:product_id, Product.all.map {|product| ["#{product.code} - #{product.name}", product.id] }, { :include_blank => 'Digita el código o nombre del producto' } ,class:"custom-select") %>
        </div>
      </div> 
      
      <div class="col-3"><br>
        <div class="field st-price">
          <%= form.label :Precio_bruto_unitario %><br>
          <%= form.number_field :price, class: "form-control form-control" %>
        </div>
      </div>

      <div class="col-3"><br>
        <div class="field">
          <%= form.label :Cantidad %>
          <%= form.number_field :quantity, class: "form-control"  %>
        </div>
      </div> 
    </div>
    <% end %>
  </div>
