<%= form_with(model: adquisition_cost, local: true) do |form| %>
  <% if adquisition_cost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adquisition_cost.errors.count, "error") %> prohibited this adquisition_cost from being saved:</h2>

      <ul>
      <% adquisition_cost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-4">
    <%= form.label :Producto, class:"field" %><br> 
    <%= form.select(:product_id, products_with_stock.all.collect {|product| ["#{product.code} - #{product.name} - #{product.stock}", product.id] }, {include_blank: "Digita producto o código"}, {class: "form-control select2-simple-dropdown list-products product"})%>
  </div>

  <div class="form-group col-4">
    <%= form.label :Costo %><br>
    <%= form.text_field :cost, class:"form-control" %>
  </div>

  <div class="actions offset-1">
    <%= form.submit "Agregar costo", class:"btn btn-info"%>
  </div>
<% end %>
