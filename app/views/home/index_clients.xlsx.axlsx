wb = xlsx_package.workbook
wb.add_worksheet(name: "Clients") do |sheet|
sheet.add_row ['Id', 'Nombre', 'Código', 'Id ciudad', 'Ciudad']
  @clients.each do |client|
    sheet.add_row [client.id]
  end
end
