/* $(document).on('turbolinks:load', function(){
  $(function() {
    $('input[name="daterange"]').daterangepicker({
      locale: {
        format: 'DD/MM/YYYY'
        }
    }, function(start, end, label) {
      console.log("Fechas seleccionadas: desde " + start.format('DD-MM-YYYY') + ' hasta ' + end.format('DD-MM-YYYY'));
    });
  });
});
*/